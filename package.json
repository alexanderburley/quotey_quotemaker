{
  "name": "quotey_quotemaker",
  "version": "0.1.0",
  "description": "Application to get a quote from a pre-existent datastore and send it somewhere",
  "main": "main.js",
  "scripts": {
    "start": "node main.js",
    "test": "mocha --recursive ./**/*.spec.js ",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4",
    "wikijs": "^4.11.2"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "eslint": "^5.14.1",
    "mocha": "^5.2.0",
    "sinon": "^7.2.3"
  }
}
